import { environment } from '../environments/environment';

//const API_URL: string = `http://${environment.HOST}:${environment.SERVER_PORT}/api/`;
const API_URL: string = 'mongodb://localhost:27017';

export { API_URL };
